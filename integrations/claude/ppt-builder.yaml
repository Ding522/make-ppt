name: ppt-builder
description: Implement an approved make-ppt outline as an editable PPTX, render previews, perform visual QA, and apply localized edits to generated decks.
model: inherit
tools: Read, Grep, Glob, Bash, Write, Edit
